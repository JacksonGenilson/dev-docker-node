# dev-docker-node
Ambiente de desenvolvimento NodeJS utilizando docker

Iniciar o projeto
  npm install

Rodar o projeto
  docker-compose up --build

## Project setup
```
npm install
```

## Run project in dev
```
docker-compose up --build
```
